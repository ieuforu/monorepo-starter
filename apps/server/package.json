{
  "name": "server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "until bun run --hot --env-file=./.env src/index.ts; do echo '依赖项尚未就绪，1秒后重试...'; sleep 1; done",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "@repo/db": "workspace:*",
    "@repo/utils": "workspace:*",
    "@repo/validators": "workspace:*",
    "hono": "^4.10.8"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "^1.3.4",
    "typescript": "^5.9.3"
  }
}
