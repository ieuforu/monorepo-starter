{
  "extends": "@repo/typescript-config/base.json",

  "compilerOptions": {
    "lib": ["ES2022"],
    "outDir": "dist",
    "rootDir": "src",
    "baseUrl": ".",
    "composite": true
  },

  "include": ["src"],
  "exclude": ["node_modules", "dist"],

  "references": [
    // 如果 db 依赖于 @repo/validators
    // { "path": "../validators" }
  ]
}
